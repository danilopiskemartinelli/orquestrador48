server {
    listen 80;

    location /api/ {
        proxy_pass http://stats-api:8000/;
        proxy_read_timeout 15s;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
